import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// This is where your root element should be rendered
ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
